# Reference: Build State Persistence

**Used by**: `/sdd.build` state rules.

## State Persistence

**Resume**: `/sdd.build --resume` loads from `state.json`
**Next**: `/sdd.build --next` finds first pending task

---
